{
  "name": "arguebuf",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "arg-services": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^4.9.4",
    "vite": "^4.0.2",
    "vitest": "^0.26.0"
  }
}